0. Terminal
Shell, bash, command prompt
linux
linux commands
ls - list
cd - change directory
.. - previous directory
clear

~ (tilda) root directory

1. git
- what is git
a tool  used to trak changes to files

i. make sure that u r in ur project folder
ii. type the command git init. u only need to do this once.
repository / repo -folder
add - preparing to save
commit - save

git status
git add .
git commit -m "something meaning"

git log

iii. set up github acct 
iv. create a new repo on github, copy paste the 3 commands, u only need to do this once

v. git push